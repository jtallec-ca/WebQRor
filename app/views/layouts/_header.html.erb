<nav class="navbar navbar-static-top navbar-default" role="navigation" id="headerbar">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: "navbar-brand" do %>
        <font  class="titleWebQ PrimColorWebQ">Web</font><font class="titleWebQ AltColorWebQ">Q</font>
      <% end %>
      <!--<a class="" href="#">WebQ</a>-->
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if not user_signed_in? %>
        <li><%= link_to "Home", root_path %></li>
        <% end %>
        <% if user_signed_in? %>
          <% if can? :manage, :all %>
            <li><%= link_to "Dashboard", admin_path %></li>
          <% end %>
          <li class="dropdown">
            <%= link_to '#', class: 'dropdown-toggle', data: { toggle: "dropdown"} do %>
              <span class="glyphicon glyphicon-user"></span>&nbsp;<%= current_user.email %>
              <b class="caret"></b>
            <% end %>
            <ul class="dropdown-menu">
              <li>
                <%= link_to edit_user_registration_path do %>
                  <span class="glyphicon glyphicon-cog"></span>&nbsp;Account Settings
                <% end %>
              </li>
              <li class="divider"></li>
              <li>
                <%= link_to destroy_user_session_path, method: :delete do %>
                  <span class="glyphicon glyphicon-log-out"></span>&nbsp;Sign out
                <% end %>
              </li>
            </ul>
          </li>
<!--           <li>
  <%= link_to destroy_user_session_path, method: :delete do %>
    <span class="glyphicon glyphicon-log-out"></span>&nbsp;Log out
  <% end %>
</li> -->
        <% else %>
          <li>
            <%= link_to new_user_session_path do %>
              <span class="glyphicon glyphicon-log-in"></span>&nbsp;Sign in
            <% end %>
          </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>